version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "taskdef-php.json"
        LoadBalancerInfo:
          ContainerName: "php"
          ContainerPort: 80


